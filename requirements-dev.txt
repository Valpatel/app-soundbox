# Development dependencies
# Install with: pip install -r requirements-dev.txt

# Testing
pytest>=7.0
pytest-cov>=4.0

# Playwright for E2E tests (optional)
# Install separately: npx playwright install
