# Sound Box Dependencies
#
# WARNING: This file installs CPU-only PyTorch. For GPU support, run:
#   ./setup.sh
# which auto-detects your GPU and installs the correct CUDA-enabled PyTorch.
# Only use this file for reference or CI environments without GPU.

# Core framework
flask>=2.0
flask-limiter>=3.0
requests>=2.28

# PyTorch (CPU version - use setup.sh for GPU/CUDA version)
torch>=2.0
torchaudio>=2.0

# Audio generation
audiocraft

# Audio analysis and visualization
numpy>=1.20
librosa>=0.10
matplotlib>=3.5
soundfile>=0.12
scipy>=1.10

# AudioCraft dependencies
einops
sentencepiece
hydra-core
hydra-colorlog
num2words
transformers
encodec
torchmetrics
protobuf
av
flashy
julius
lameenc
demucs
xformers
spacy

# Piper TTS
piper-tts
onnxruntime  # Use onnxruntime-gpu for NVIDIA GPU acceleration

# Scheduling
apscheduler>=3.10

# Configuration
python-dotenv>=1.0
